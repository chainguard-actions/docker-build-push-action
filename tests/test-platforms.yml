name: Test build with platforms
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up QEMU for multi-platform builds
        run: |
          docker run --rm --privileged tonistiigi/binfmt:latest --install all 2>/dev/null || true

      - name: Set up Docker Buildx
        run: |
          docker buildx version || true
          docker buildx create --name test-builder-platforms --use --bootstrap 2>/dev/null || \
            docker buildx use test-builder-platforms 2>/dev/null || true
          docker buildx inspect --bootstrap

      - id: build
        uses: .
        with:
          context: .
          file: tests/fixtures/Dockerfile.platforms
          push: 'false'
          tags: test-platforms:latest
          platforms: linux/amd64
          outputs: type=local,dest=/tmp/test-platforms-output
          provenance: 'false'
          sbom: 'false'
        env:
          DOCKER_BUILD_SUMMARY: 'false'
          DOCKER_BUILD_RECORD_UPLOAD: 'false'

      - name: Assert imageid is set
        env:
          IMAGEID: ${{ steps.build.outputs.imageid }}
        run: |
          echo "imageid: $IMAGEID"
          if [ -z "$IMAGEID" ]; then
            echo "FAIL: imageid output is empty — build with platforms may have failed"
            exit 1
          fi
          echo "PASS: imageid is set to $IMAGEID"

      - name: Assert output directory exists
        run: |
          if [ ! -d /tmp/test-platforms-output ]; then
            echo "FAIL: output directory /tmp/test-platforms-output not found"
            exit 1
          fi
          echo "PASS: output directory exists"
          ls -la /tmp/test-platforms-output/
