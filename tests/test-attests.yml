name: Test Docker build with attests input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create minimal Dockerfile
        run: |
          mkdir -p /tmp/docker-attests-ctx
          cat > /tmp/docker-attests-ctx/Dockerfile <<'EOF'
          FROM alpine:3.18
          RUN echo "attests test"
          EOF

      - id: build
        uses: .
        with:
          context: /tmp/docker-attests-ctx
          file: /tmp/docker-attests-ctx/Dockerfile
          tags: test-attests:latest
          load: 'true'
          push: 'false'
          attests: type=provenance,mode=min

      - name: Assert image was loaded
        run: |
          if ! docker image inspect test-attests:latest > /dev/null 2>&1; then
            echo "FAIL: image test-attests:latest not found in local Docker"
            exit 1
          fi
          echo "PASS"

      - name: Assert metadata is non-empty
        env:
          METADATA: ${{ steps.build.outputs.metadata }}
        run: |
          echo "metadata: $METADATA"
          if [ -z "$METADATA" ]; then
            echo "FAIL: metadata output is empty"
            exit 1
          fi
          echo "PASS"
